- Make sort generic for any number of columns (Not required for this project)
- Support sort for column value types string and integer
    - Resolve merge conflicts with Devaki's PR
- Make sure that all the malloc'ed memory in TournamentTree is freed in its Deconstructor
- Introduce use of vectors for list
- Run valgrind on the final implementation of External Sort
- Implement SSDDevice and HDDDevice class
- Need to change vector attributes allocation to mitigate with invalid free crashes
- Add disk writes and reads number and add code to get latency for External Sort